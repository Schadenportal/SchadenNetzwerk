export type CarModel = {
  id: number,
  make_id: number,
  name: string,
}

export type IDamageTableFilters = {
  damageNumber: string;
  customerName: string;
  licensePlate: string;
  orderStatus: string;
  serviceAdviser: string;
  repairApprovalStatus: string;
}
